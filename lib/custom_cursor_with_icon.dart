/// A Flutter TextField widget with an animated cursor that displays a
/// customizable icon below the caret.
///
/// This package provides [CustomCursorTextField], a drop-in replacement for
/// Flutter's [TextField] that renders a custom animated cursor with a
/// decorative icon positioned below the caret line.
///
/// ```dart
/// CustomCursorTextField(
///   icon: Icon(Icons.edit, size: 12, color: Colors.blue),
/// )
/// ```
library;

export 'src/custom_cursor_text_field.dart';
