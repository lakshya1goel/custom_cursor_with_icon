name: custom_cursor_with_icon_example
description: Example app demonstrating the custom_cursor_with_icon package.
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  custom_cursor_with_icon:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
